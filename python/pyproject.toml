[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "oyyo-sdk"
version = "0.1.0"
description = "Python SDK for OYYO"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
